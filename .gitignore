# python
__pycache__/
*.py[cod]
*.so
*.egg-info/
build/
dist/
.ipynb_checkpoints/

# env / editor noise
.venv/
.DS_Store

# NOTE: experiment outputs (results/, *.log) and project docs (RESULTS_NOTES.md,
# SETUP.md) are intentionally tracked so the repository captures the full
# experiment record alongside the code.
